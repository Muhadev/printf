#include "main.h"

/**
 * _string - allocate a string to stdout
 * @p: pointer to the string
 * Return: chars to print
 */

int _string(char *p)
{
int n;
for (n = 0; p[n] != '\0'; n++)
{
	_putchar(p[i]);
}
return (n);
}
