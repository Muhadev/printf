#include "main.h"

void printf_design(const char *string_format, slen_tp *Plot, ...)
{
va_list info;
if (prm -> l_modifier)
{
return(handle_length_l(string_format, info);
}
else if (prm -> h_modifier)
{
return(handle_length_h(string_format, info);
}
else
{
return(handle_length_l(string_format, info);
}
}
